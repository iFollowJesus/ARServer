// app.js

const runPythonScript = require('./services/pythonRunner'); // Adjust the path as necessary

const scriptPath = './pythonScripts/genVar.py'; // Replace with the actual path
runPythonScript(scriptPath);
